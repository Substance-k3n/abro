# balances

Derived balance engine — never a stored/manually-updated balance. `docs/ABRO_PRD.md` §16, §18 (debt simplification).

Not yet implemented — this directory is reserved so the module boundary exists from the start.
